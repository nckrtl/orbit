# ORB-352 development evidence

Current candidate: 0f97c9a97623018982a8579568f5d3e3c445a46a. Included main:035957948da10687c0cac9366d0d957a28aa701a. Flow:proof, explicitly requested by the user. Incus:required. This record describes publication inputs; subsequent captures, exports and independent review stay in the harness and external delivery report.

The contributor workflow on current main governs feature delivery. Its CI and independent Incus review remain required. The task preserves its additional explicit isolated-proof request and resource cleanup safeguards. The historical plan and plan receipt are retained as design evidence; the removed plan-lint command is not a current contribution prerequisite.

## Result and scope

The shared CLI helpers separate input admission, machine output, decoration, repainting and selected-output width. Scoped Laravel Prompts v0.3.24 supplies validation, retry, typed aborts and stable-key datatables. HumanRenderer preserves complete values, literal markup, null/false/zero, every table column, detail/property/error structure and narrow-width fallback. ProgressDisplay and SpinnerDisplay maintain one output owner through nesting, acknowledge paint before parent callbacks and retain a300ms monotonic animation clock. Explicit cleanup preserves terminal settings, cursor visibility and owned processes on success, failure and interruption.

GatewayCommand, GatewayFailureRenderer and Kernel integrate common boundaries. Commands continue to own targets, admitted prompts, consent, force meanings, transport, statuses and schemas. No new public flags or API/SDK fields are introduced by this feature, and no harness code changes. Business callbacks execute exactly once in the parent.

Main's database-query change adds four public commands and updates the registry and contracts. It was integrated unchanged. The subsequent contributor-workflow change removed designing-cli-commands; the user explicitly requires that focused skill. It and its adoption template are restored and linked from current root, development and review guidance, without restoring obsolete phase gates. verifying-cli-output follows the simplified maintained guide and keeps its recorder tools.

## Checks

Current source manifest:2686committed files, SHA256774389a768953a9791054938238f7ddb60cb664276d7c9c19cfdf43a77e2271e. Mounted discovery uses the harness marker and full blob manifest; isolated proof uses clean Git identity.

Skill validation passed with the skill-creator validator in an isolated uv environment. Docs build and lint passed after the latest integration: /tmp/orb352-docs-build-new-flow.log and /tmp/orb352-docs-lint-new-flow.log. Generated documentation context is current. Current-head Beast checks and CI are recorded externally as they complete; publication is not proof that pending checks passed.

Earlier536beab9 passed all15Beast checks, original /home/nckrtl/orbit/.git/orbit-checks/536beab93888d2ff21ac7e7f36c7561f37516ea9/review-ghg2z_li/result.json. Its CLI TIA executed47tests/1984assertions for CommandSurfaceTest and DatabaseConnectionCommandsTest. Earlier05f5bd9e correction TIA executed779tests/5297assertions. Named earlier receipts in checks/ remain historical, not gates for this head. E2E test:fresh passed1553tests/8645assertions at1b91c68a during fixture development; current-head CI and local checks cover subsequent upstream harness/test guidance changes.

## Proof cases

The six declared actions cover layout34 plus12state-style cases, modes14, prompts16, liveness5, lifecycle21 and contracts27. Each retains exact invocations, descriptor facts, timestamped bytes, frames, input events, shell/process facts and asserted negative outcomes. The normalized proof plan is unchanged.

Observed inputs:false. These local CLI/PTY fixtures cannot supply Gateway CLI and FPM observations in both phases. Broad static inputs remain declared. mutates:true covers disposable recorder setup. All actions must exit zero after asserting their child results. New code or fixture behavior gets new recordings.

Full archives are exported through the supported recorded shell passthrough. The external exporter verifies guest source identity, archive size/hash, the complete decoded transfer and all manifest entries. The transport was compared byte-for-byte with the earlier750bounded-read export: same1507632bytes and SHA256b77729ab8fcaaa4040b41d21682358bb8af7e5c51c2587dfe07e6899093f370e, transferred in1.175seconds. It adds no network listener or harness changes. A prior optional private-network upload experiment timed out and remains a failed exploratory action; it is not acceptance evidence.

## Historical findings and retained evidence

Nested-renderer recordings exposed40–155ms glyph changes. The renderer now preserves its clock and final active glyph; same-stream callback-entry markers prove paint precedes work.

Native Solo inspection found displaced Unicode table separators atfe66e4f5 despite an automated pass. Both implementation and original oracle assumed a two-cell emoji cluster. Cursor queries showed four cells for joined/modifier emoji and one for keycaps. Ordinary base-character measurement now preserves complete graphemes. The corrected independent oracle rejects all eight old overflowing tables.

Candidateaf717b84 then passed all six isolated actions in attemptd36360e0c0d60a6ba9a5ad135ec1f83d. Its complete129recordings/1924files were exported and hash-verified. Native Solo additionally passed stable selection, invalid-text retry, Ctrl-C without mutation, 60/80/120column alignment, the25/24table/fallback boundary and nested progress. Its82manual files were also exported and verified. Terminal settings were restored; no collector errors or missing bytes were accepted.

That successful attempt was superseded by incoming database and contributor-workflow changes. The harness equivalence report required complete reproof. Captured proof, review record, all archives and checks remain under /Users/nckrtl/.codex/work/cli-ux-restoration/evidence/orb352-proof-d36360e0 before supported replacement release. Earlier99235e68 anddce9c6e7 failures are preserved in their own directories.

## Documentation and remaining adoption

- docs/reference/cli-ux.md: helper behavior, complete layouts, ordinary terminal cells and invocation boundaries.
- docs/cli/overview.mdx: common surfaces and current format, target and consent boundaries.
- docs/generated/context.json: generated context, reconciled with current main.

The historical plan records the bounded documentation audit and family-owned findings. Implementation clarifications concern plain waiting lines, invocation scoping, minimum datatable height, nested cadence and native width. Current command contracts keep authority over product behavior.

The live registry was reconciled with Herdr enabled in a private temporary home:112public and20framework/internal commands. Root command-inventory-current.json and command-matrix-current.md account for every public command with valid source/test paths. All112adoption verdicts remain Unverified. ORB-353–362 own family migrations; the database family needs the four newly landed commands included before it starts. ORB-363 owns integrated adoption. Shared primitives alone do not establish compliance.

## Review corrections at the current candidate

Independent review found invisible plain prompt updates and external-signal terminal cleanup failures. Plain prompts now append changed frames for user input, selection and search. Scoped SIGINT/SIGTERM handlers unwind prompts; InputTerminal temporarily reads without blocking and restores the caller stream mode. Real PTY checks exposed and corrected PHP blocking-read restart before this candidate. Prior signal handlers and async state restore with the prompt context.

Local CLI TIA passed 1022 tests and 6745 assertions; project quality checks passed. Docs build and lint passed. Current source manifest contains 2686 committed files. Root Beast checks and CI run against this new candidate; their final receipts stay with the review evidence.

The prompt proof now contains 25 cases, including visible plain selection/search/editing, external SIGINT/SIGTERM and recorder timeouts for text and password prompts. Native terminal settings are compared before collector recovery. The local macOS preflight preserves raw flags and records a transient PENDIN difference; Incus uses exact termios equality without that local normalization. All 25 local preflight cases passed with that stated platform limitation.

Snapshot refresh from clean Beast main succeeded, generation035957948da1-4b69a63622ea. Discovery619cb3e0 is task-owned; fresh proof for this candidate remains to run.
