# Feature plan

Issue: ORB-132
Review verdict: FIX

## Outcome

An issue proof plan can add one attempt-local `app-prod-2` Node to discovery and proof while the shared topology snapshot and standard three-Node flow stay unchanged.

## Code boundaries

In:
- Declaration, addressing, and persisted topology identity: extend `apps/e2e/app/E2E/Value/ProofPlan.php`, `TopologyEndState.php`, `TopologyRecipe.php`, `TopologyTarget.php`, `FeatureTopology.php`, `ProofResult.php`, `ProofFixtures.php`, and the new typed extension/construction-input values beside them. The optional normalized plan field is `"extension": "app-prod"`; it selects the physical Node key `app-prod-2`, Incus address `.13`, WireGuard address `10.44.0.4`, and forces `mutates: true`. No extension field preserves the current serialized plan and fingerprint.
- Plan loading and mixed-source construction: update `apps/e2e/app/E2E/ProofPlanFile.php`, `TopologyAcquirer.php`, `TopologyProofRunner.php`, `HostCapacity.php`, `DiscoveryGuestPreparer.php`, `IncusNetworkLifecycle.php`, `app/Providers/AppServiceProvider.php`, and a shared `apps/e2e/app/E2E/IssueTopologyConstructor.php`. The constructor reserves the selected recipe size before mutation, copies only the three registered Nodes from the pinned generation, creates only `app-prod-2` from the configured generic image, records the observed image fingerprint and full intended inventory, and rolls that inventory back on failure. `topology:acquire` reads the active issue plan when present before it creates the discovery lease or any Incus resource.
- Convergence and verification: make `apps/e2e/app/E2E/TopologyConverger.php`, `TopologyVerifier.php`, `WorktreeSynchronizer.php`, `ProofFixtureStager.php`, `ProofInputManifestBuilder.php`, `ProofEquivalenceEvaluator.php`, `resources/guest/converge-app-prod-internal-tls.sh`, and `resources/guest/verify-topology.sh` operate on physical recipe Nodes. Provision and converge both app-prod Nodes through the existing product command, but keep the sample AppInstance on the original `app-prod` Node; verify `app-prod-2` network identity, app-prod role, PHP-FPM, Caddy, and fixture/runtime execution without creating a second sample placement.
- Command targeting, retained evidence, and exact cleanup: update `apps/e2e/app/Console/Commands/Topology/{Acquire,Exec,Shell}.php`, `apps/e2e/app/E2E/Value/ProofInputManifest.php`, `IssueState.php`, `TopologyReleaser.php`, and `TopologySnapshotPromoter.php`. Commands and proof actions accept only physical keys from the recorded recipe; topology and proof-input schemas bind the extension, source generation, image alias and fingerprint, ordered four-Node identities, and action results; equivalence and promotion revalidate them; release uses the persisted inventory and keeps the lease after an ownership conflict or partial failure.
- Public harness and closeout guidance: update `bin/e2e-topology`, `.agents/skills/developing-features/SKILL.md`, `.agents/skills/merging-pull-requests/SKILL.md`, and `apps/e2e/tests/Feature/Configuration/AgentRoleContractTest.php`. The developer writes the extended proof declaration before acquisition, and closeout treats its normalized mutating proof as refresh-only, records the required handoff identities, and releases proof and discovery only after refresh succeeds.
- Focused coverage stays in the issue-named tests: `apps/e2e/tests/Unit/E2E/Value/{ProofPlan,TopologyRecipe,FeatureTopology,ProofEvidenceValue}Test.php`, `apps/e2e/tests/Unit/E2E/{HostCapacity,TopologyAcquirer,TopologyConverger,TopologyVerifier,TopologyProofRunner,ProofFixtureStager,ProofInputManifestBuilder,ProofEquivalenceEvaluator,TopologyReleaser,TopologySnapshotPromoter}Test.php`, and `apps/e2e/tests/Feature/Commands/{TopologyCommands,TopologySnapshotCommands}Test.php`.

Out:
- Product behavior stays unchanged: do not edit `apps/gateway/`, `apps/cli/`, or `packages/php-sdk/`. The harness calls their supported Node provisioning and role-convergence operations but adds no production Route-pool behavior, application-placement policy, fleet migration, or session-store setup.
- The shared snapshot stays the exact registered three-Node profile: keep `TopologyProfile`, `TopologySnapshotGeneration`, snapshot identities, refresh/rebuild/cold-replacement construction, and their manifests at Gateway, app-dev, and one app-prod Node. An extended attempt never copies, removes, promotes, or writes `app-prod-2` into the shared snapshot.
- Do not add Gateway or app-dev Nodes, an arbitrary recipe field, a second extension, or more than one extra Node. Reject every extension value other than `app-prod` before a lease, capacity reservation, network, or VM is created.
- Do not change `bin/e2e-scenarios`, `apps/e2e/tests/Scenario/**`, or the disposable cold-scenario authority. Scenario results remain regression evidence and never substitute for the three required issue proof actions.
- Production deployment, production resources, and production release remain outside this harness issue.

## Documentation

- Audit scope: ORB-132 with component `apps/e2e` and concepts Gateway, Node, AppInstance, Route, and Proof topology. The issue-scoped audit found no drift outside the outcome pages and reported no owner follow-up.
- `docs/reference/incus-topologies.md`: now states the declaration, fixed `app-prod-2` identity and addresses, mixed construction, four-VM capacity, physical command targeting, diagnosis access, persisted inventory, retry, and exact release behavior.
- `docs/reference/proof-plans.md`: now states the `extension` field, forced mutation, physical action and end-state validation, fixture staging, construction phase, retained construction inputs, and stale-equivalence rule.
- `docs/reference/topology-snapshot.md`: now states that the shared inventory remains three Nodes, direct promotion refuses extended proof, and closeout refreshes before recording and releasing both attempts.
- `docs/generated/context.json`: rebuilt so ADR 0040 routes to the Incus topology reference.
- Documentation commits: `c3ddf236` (`docs: define extended issue topology contract`) and `c32b58fe` (`docs: clarify physical proof inventories`). Verification: `composer docs-build` and `composer docs-lint` passed.

## Acceptance map

| Criterion | Boundary | Focused proof |
| --- | --- | --- |
| Standard plans remain byte-for-byte normalized while `extension: app-prod` selects exactly one valid fourth physical Node and rejects unsupported declarations before mutation. | `Value/ProofPlan.php`, `TopologyEndState.php`, `TopologyRecipe.php`, `TopologyTarget.php`, `FeatureTopology.php`; acquire command/loader | `apps/e2e/tests/Unit/E2E/Value/ProofPlanTest.php`; `apps/e2e/tests/Unit/E2E/Value/TopologyRecipeTest.php`; `apps/e2e/tests/Feature/Commands/TopologyCommandsTest.php` |
| Discovery and proof clone the pinned three-Node generation, independently construct and converge `app-prod-2` from the recorded generic image, and never adopt the other attempt's VM. | `IssueTopologyConstructor.php`, `TopologyAcquirer.php`, `TopologyProofRunner.php`, `TopologyConverger.php`, construction-input state | Incus proof action `extended-topology-ready`; `apps/e2e/tests/Unit/E2E/TopologyProofRunnerTest.php` |
| Discovery stays the default target and explicit physical keys drive command execution, fixture staging, verification, and action evidence without shared-role ambiguity. | `Console/Commands/Topology/{Exec,Shell}.php`, `TopologyAcquirer.php`, `ProofPlan.php`, `ProofResult.php`, `ProofFixtureStager.php`, `TopologyVerifier.php` | Incus proof action `extended-node-targeting`; `apps/e2e/tests/Feature/Commands/TopologyCommandsTest.php` |
| Each extended attempt reserves four VM slots before resource creation and frees capacity through rollback or release. | `HostCapacity.php`, `IssueTopologyConstructor.php`, `TopologyAcquirer.php`, `TopologyProofRunner.php` | `apps/e2e/tests/Unit/E2E/HostCapacityTest.php`; `apps/e2e/tests/Unit/E2E/TopologyAcquirerTest.php` |
| Retained proof binds candidate, normalized extension, pinned generation, current extra-image fingerprint, all physical resources, proof inputs, and complete zero-exit actions; a changed construction input is stale. | `FeatureTopology.php`, `ProofResult.php`, `ProofInputManifest.php`, `ProofInputManifestBuilder.php`, `ProofEquivalenceEvaluator.php`, `TopologyProofRunner.php` | `apps/e2e/tests/Unit/E2E/ProofInputManifestBuilderTest.php`; `apps/e2e/tests/Unit/E2E/ProofEquivalenceEvaluatorTest.php`; `bin/e2e-topology prove ORB-132` with the extended `.loop/proof/ORB-132.json` |
| Construction and verification failure retain the full owned inventory for supported diagnosis, while exact cleanup refuses conflicts and preserves unrelated resources. | `IssueTopologyConstructor.php`, `TopologyProofRunner.php`, `IssueState.php`, `TopologyAcquirer.php`, `TopologyReleaser.php`, topology commands | `apps/e2e/tests/Unit/E2E/TopologyProofRunnerTest.php`; `apps/e2e/tests/Unit/E2E/TopologyReleaserTest.php`; `apps/e2e/tests/Feature/Commands/TopologyCommandsTest.php` |
| A proved extended attempt rejects sync, exec, and an end-state declaration that removes only `app-prod-2`; it remains intact through review. | `ProofPlan.php`, `TopologyEndState.php`, `TopologyAcquirer.php`, `TopologySnapshotPromoter.php` | `apps/e2e/tests/Unit/E2E/TopologyProofRunnerTest.php`; `apps/e2e/tests/Feature/Commands/TopologyCommandsTest.php` |
| Extension forces mutation, direct promotion fails before Incus access, and merge closeout refreshes from merged main before release; failed refresh leaves both attempts active. | `ProofPlan.php`, `TopologySnapshotPromoter.php`, `.agents/skills/merging-pull-requests/SKILL.md` | `apps/e2e/tests/Unit/E2E/Value/ProofPlanTest.php`; `apps/e2e/tests/Unit/E2E/TopologySnapshotPromoterTest.php`; `apps/e2e/tests/Feature/Commands/TopologySnapshotCommandsTest.php`; `apps/e2e/tests/Feature/Configuration/AgentRoleContractTest.php` |
| Release removes both app-prod VMs, the remaining Node VMs, network, and devices from the exact record, verifies absence, and supports retry without changing snapshot inventory. | `FeatureTopology.php`, `IssueState.php`, `TopologyReleaser.php` | `apps/e2e/tests/Unit/E2E/TopologyReleaserTest.php`; `apps/e2e/tests/Feature/Commands/TopologyCommandsTest.php` |
| `app-prod-2` is reachable on the private attempt network, runs the expected app-prod services and PHP runtime, and executes a fixture without legacy records or another product placement. | `TopologyConverger.php`, `TopologyVerifier.php`, `converge-app-prod-internal-tls.sh`, `verify-topology.sh`, issue proof fixture | Incus proof action `extended-runtime-connectivity` |
| Maintained reference and generated context cover declaration, capacity, selection, input binding, diagnosis, retention, release, and closeout. | `docs/reference/incus-topologies.md`, `docs/reference/proof-plans.md`, `docs/reference/topology-snapshot.md`, `docs/generated/context.json` | `composer docs-lint` |
| The complete E2E and repository suites remain green. | All implementation and test boundaries above | `cd apps/e2e && composer check`; `bin/test` |

## Implementation order

1. Add the optional typed `app-prod` extension to the proof-plan and recipe values, derive the four physical keys and both address families, make end-state/action validation recipe-aware, and prove the no-extension serialization and fingerprint are unchanged.
2. Make the persisted feature-topology and proof-input schemas record the selected recipe, pinned generation, intended instance map, image alias, and observed fingerprint; update state reconstruction and tests before any lifecycle service consumes the new schema.
3. Extract the duplicated discovery/proof resource transaction into `IssueTopologyConstructor`: validate the plan and image first, reserve the recipe count, create the private network, copy the three pinned snapshots, construct `app-prod-2`, and roll back the full intended inventory on any failure. Keep candidate convergence on the standard recipe.
4. Integrate the constructor into acquire and prove. For extended discovery only, run full extension-aware convergence after mounting the worktree; keep the standard discovery path unchanged. Record construction failure before lease removal only when exact rollback succeeds.
5. Generalize convergence, WireGuard provisioning, and verification from singleton app-prod role lookup to ordered physical Nodes. Provision both app-prod Nodes, but keep sample resources and hydration on the original Node; add service and network probes for `app-prod-2`.
6. Make fixture staging, proof actions/results, exec/shell diagnosis, immutable guards, and release use the persisted physical inventory. Verify retryable exact cleanup and conflict refusal across all four VMs, mounts, and the network.
7. Bind the topology construction inputs into the immutable manifest and equivalence evaluation, retain complete action evidence, and make promotion refuse every extended plan before mutation. Update developer and closeout guidance for early declaration and refresh-only closeout with release after successful refresh.
8. Write `.loop/proof/ORB-132.json` with `extension: app-prod` and the three Incus actions `extended-topology-ready`, `extended-node-targeting`, and `extended-runtime-connectivity`, plus only the flat fixture needed to inspect the two app-prod Nodes and run PHP on `app-prod-2` without creating an AppInstance.
9. Run the named focused tests, `cd apps/e2e && composer check`, `composer docs-lint`, and `bin/test`; then prove the exact clean commit with `bin/e2e-topology prove ORB-132` while extended discovery remains the default command target.

## Must preserve

- ADR 0005: the persistent profile and coordinated snapshots remain `gateway`, `app-dev`, and `app-prod`; snapshot state is an acceleration cache whose exact manifests and objects fail closed; normal refresh starts from the promoted generation; promotion and recovery remain deterministic; Incus development never changes production release authority.
- ADR 0006: discovery and proof keep separate attempt IDs, networks, instances, devices, inventories, and state; discovery remains the default command target; proof starts fresh, synchronizes one exact candidate, requires every action to exit zero, and turns post-construction failures into diagnosis; proved topology stays immutable; cleanup revalidates exact ownership and never selects by prefix, age, or broad query; production resources remain separate.
- ADR 0015: proof reuse depends on recorded-input equivalence, not SHA or ancestry alone; the immutable manifest and compact result fingerprints remain mutually checked; the plan and all active fixtures remain proof-contract inputs; unknown or changed runtime/construction inputs fail closed; equivalence stays explainable; review, merge, promotion, and lineage remain bound to exact proved, accepted, included-main, and merged identities; Pest remains required for the combined candidate.
- ADR 0019: physical Node keys remain separate from assigned roles; deterministic names, MACs, addresses, capacity, and exact cleanup use the selected recipe; the scenario lanes, cold constructor authority, and scenario evidence stay independent and cannot become issue acceptance or promotion authority.
- ADR 0035: mutating-proof closeout runs snapshot refresh only after the external merge and current `origin/main` are verified; records proved attempt, accepted head, merge commit, and promoted generation; releases discovery and proof only after refresh promotion; never substitutes refresh for missing, failed, or non-equivalent proof; never edits refresh lineage.
- ADR 0036: the harness provisions supported samples only through AppInstance and Route contracts, creates no legacy Instance or Workspace for the extra Node, and adds no migration facility or compatibility path.
- ADR 0037: the shared snapshot remains one fresh three-Node generation; normal discovery and proof clone it; issue extension is not a cold snapshot replacement; the existing generation remains protected until an authorized refresh or promotion completes; ordinary scenarios gain no acceptance or promotion authority.
- ADR 0040: only discovery and proof may declare exactly one additional app-prod Node before construction; they clone the standard three Nodes and construct the extra Node inside the same isolated attempt; extension, construction inputs, and complete inventory are bound to evidence; candidate identity, convergence, acceptance, diagnosis, immutability, ownership, capacity, recovery, and cleanup cover the extra Node; extended proof is non-promotable; closeout refresh preserves the three-Node snapshot and releases both full attempts only after success; the extra Node is never removed to manufacture promotable evidence; scenario evidence is never substituted.
- Existing adjacent invariants: standard plan normalization/fingerprints and three-VM acquisition remain unchanged; the topology snapshot serializers accept only their fixed inventory; checkout source exists only on Gateway and app-dev; shared-role singleton lookup remains fail-closed unless code explicitly iterates physical Nodes; commands reject unrecorded keys; proved attempts reject mutation; ownership conflicts keep the lease and unrelated Incus resources; every cleanup verifies absence.

## Open questions

none

## Deviations

none

## Review findings

- FIX — Acceptance item 2 requires both extended discovery and extended proof to construct their own `app-prod-2` from the recorded generic image and never adopt the other attempt's VM. The mapped `extended-topology-ready` action runs only inside proof, and `TopologyProofRunnerTest.php` covers only the proof lifecycle; the row names no focused evidence for discovery construction or for distinct discovery/proof identities and inventories. Add a `TopologyAcquirerTest.php` boundary that exercises extended discovery construction and proves the two attempts derive separate VM identities and addresses without adoption, then map that proof in the row.
- FIX — Acceptance item 10 requires the retained Incus action to prove the extra Node's runtime and the absence of legacy Instance or Workspace creation, production AppInstance placement on `app-prod-2`, and multi-target routing. Step 8 only says the fixture inspects the Nodes and runs PHP without creating an AppInstance; that action can exit zero even if convergence accidentally created one of the forbidden records or placed/routed the sample on the extra Node. Define a named self-checking fixture and exact action boundaries that inspect the Gateway's post-convergence graph, require the sole sample placement to remain on `app-prod`, require no legacy records or extra-Node Route target, and also verify `app-prod-2` connectivity, PHP, PHP-FPM, and Caddy. Bind its successful and refusal cases to focused automated tests.
- FIX — Acceptance item 10 and the `docs` label require the maintained pages to state the delivered runtime behavior and its limits. The changed extension sections document identity, addresses, role, fixture staging, and lifecycle, but they never state that `app-prod-2` receives active app-prod services and a usable PHP runtime while the sample AppInstance remains on `app-prod`, and that the extension creates no legacy Instance or Workspace and adds no production AppInstance placement or multi-target routing. Add these observable behaviors and limits to the owning reference page, update the Documentation section, and rerun `composer docs-build` and `composer docs-lint`.
