---
paths:
  - 'tests/**'
---

# Tests

## Use Pest 5 and project quality checks
Develop behavior with focused Pest 5 tests. Before delivery, run focused tests, Rector, Pint format checks and Larastan analysis, and git diff --check; report exact test and assertion counts. Require the reviewer's passing root `composer check` receipt for the exact candidate.
