---
paths:
  - 'tests/**'
---

# Test rules

Use Pest with focused unit and configuration tests. Test exact-ID validation,
safe path handling, redaction, atomic persistence, and command failures. Keep
tests isolated and database-free; do not add HTTP or queue test state.
