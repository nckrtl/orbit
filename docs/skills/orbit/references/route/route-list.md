---
title: "route:list"
description: "List Routes."
---

# route:list

List every Route with its domain, kind (`app` or `custom_proxy`), provenance (`generated` or `explicit`), publication intent, scope, target or upstream, and status.

```bash
orbit route:list [--json]
```
