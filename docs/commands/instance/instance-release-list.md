---
title: "instance:release:list"
description: "List retained releases and the current selection."
---

List retained release names and the current selection. It returns no deployment history.

```bash
orbit instance:release:list <instance> [--json]
```

## Use it when

Use this command to find a release name for [`instance:rollback`](instance-rollback.md), or to confirm which release is live.
