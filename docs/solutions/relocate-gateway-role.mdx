---
title: "Relocate the gateway role"
description: "Move the Laravel/FPM control plane to another Node while vpn and private DNS stay on the original Node."
---

# Relocate the gateway role

## Problem

The Gateway Laravel app and the WireGuard/DNS server start on the same Node. Later you may want the control plane on a new VPS while `vpn` and private DNS stay where they are.

## Cause

Bootstrap assigns both `gateway` and `vpn` to one Node. `vpn` stays immutable. The `gateway` role is a singleton. Removing it and adding it again drops the only active Gateway assignment, so the next API call loses implicit Gateway authority and `gateway.orbit` disappears from private DNS until a new assignment exists.

## Solution

### Preferred: rename first, then relocate

The Node name is a unique registry identifier. It is not the WireGuard public key or address. When the current Node is named `gateway` and the new VPS must register as `gateway`, rename the old Node first while the new VPS is still unregistered:

```bash
orbit node:rename gateway vpn
orbit node:add gateway <new-vps-host>
```

`node:add` looks up an existing record by name. Rename-first avoids a unique-name collision. [ADR 0091](/decisions/0091-rename-a-node-without-changing-wireguard-identity) owns that command. Destroy any Herdr sessions on the old Node before rename; observer hostnames embed the Node name.

Then provision the new Node as an ordinary machine. It must not already hold `app-dev`, `app-prod`, or `database`; those roles conflict with `gateway`. Install PHP-FPM and Caddy there, and copy the serving checkout, `ORBIT_HOME` (CA and Gateway SSH keys), and Gateway `.env`. Relocate updates the live SQLite on the process host that runs the command, so copy that database onto the target after relocate (or copy it again if you staged one earlier). Then transfer the singleton assignment:

```bash
orbit node:role:relocate gateway gateway --force
```

Relocate keeps one active `gateway` row for the whole request. It opens `orbit:gateway-https` on the target, moves the assignment, grants the new Gateway directed access to the `vpn` node and the Metrics node, republishes private DNS on the `vpn` node so `gateway.orbit` answers with the target WireGuard address, and retracts the gateway HTTPS firewall on the source. It does not move `vpn`, stop the old Caddy or PHP-FPM, or copy secrets. [ADR 0092](/decisions/0092-publish-private-dns-on-the-vpn-node-after-gateway-relocate) owns the DNS target and the granted access set.

### Two-step fallback

Remove then add only when you can still reach the leftover serving stack by WireGuard IP (the CLI profile address). Prefer relocate.

```bash
orbit node:role:remove <old-node> gateway --force
orbit node:role:add <new-node> gateway --converge
```

Between those commands `gateway.orbit` has no active role, so private DNS omits the record. The Node that still runs the Gateway process keeps serving-host authority so the add can run. The add grants the same `vpn` and `metrics` access edges as relocate.

### After the assignment moves

1. Confirm `orbit node:list` shows names `vpn` and `gateway`. Confirm `orbit node:role:list gateway` shows `gateway` and `orbit node:role:list vpn` still shows `vpn`.
2. Confirm `orbit node:show gateway` lists directed access to the `vpn` node and, when Metrics exists, the Metrics node.
3. Copy the live SQLite from the process host that ran relocate onto the target, then start Caddy and PHP-FPM there.
4. From a peer that trusts the Orbit CA, `curl https://<new-wireguard-ip>/up` once the new serving stack is up, then `curl https://gateway.orbit/up`.
5. Point CLI profiles that stored the old WireGuard origin at the new address.
6. The leftover serving-host setting still names the original Node, so that Node keeps implicit control-plane authority until you re-bootstrap or write `gateway.serving_node_id` to the new Node.
7. Stop leftover Caddy and PHP-FPM on the old Node after the new `/up` is healthy.

Private DNS publication now SSHes to the `vpn` node when that node is not the serving Gateway. Inspect the catalog and listener there, not on the new Gateway. A Metrics `--converge` that fails while publishing DNS leaves Grafana, Prometheus, and `/etc/orbit/metrics` in place.

Peer converge and `node:add` project the WireGuard hub `PrivateKey` and `Address` onto the `vpn` node. After the roles split, that install SSHes to the `vpn` node and does not write those hub credentials onto the Gateway PHP host. The Gateway host keeps the peer configuration that already joined the mesh. Inspect `/etc/wireguard/orbit.conf` on the `vpn` node for hub identity. [ADR 0094](/decisions/0094-project-wireguard-hub-config-onto-the-vpn-node) owns that target.

`vpn` cannot be removed or added through the generic role commands.

## Limits

Relocate does not install Laravel on the target and does not rsync SQLite or the Orbit CA. Live cutover of those files is an operator step. This page does not cover pointing production clients at a new public VPS.

## Verification

```bash
orbit node:role:list <old-node>
orbit node:role:list <new-node>
orbit node:doctor <new-node>
curl --fail https://gateway.orbit/up
```

On disposable Incus, run [`apps/e2e/resources/proofs/rename-and-relocate-gateway-role.sh`](https://github.com/nckrtl/orbit/blob/main/apps/e2e/resources/proofs/rename-and-relocate-gateway-role.sh) `<source-name> <target-name>` on the Gateway checkout Node. The script renames the source to `vpn`, relocates `gateway` onto the target, then renames the target to `gateway`. Keep `vpn` on the old Node and smoke `/up` on the source WireGuard address while the leftover stack still runs. After the operator copies the serving checkout, smoke `/up` on the new address as well. After relocate, `orbit node:show` on the new Gateway must list access to the `vpn` node. To prove the gateway≠vpn DNS path, converge a Route or Metrics from the new Gateway process and confirm `orbit-private-dns` and `/var/lib/orbit/private-dns/catalog.json` change on the `vpn` node, not on the Gateway. Those proof scripts stay out of `apps/e2e/resources/guest/` so they do not join the required guest inventory.
