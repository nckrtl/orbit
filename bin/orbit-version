#!/usr/bin/env bash

set -Eeuo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." >/dev/null 2>&1 && pwd)"

cd "$repo_root"

git rev-parse --is-inside-work-tree >/dev/null 2>&1 || {
    printf 'orbit-version: not a git checkout\n' >&2
    exit 1
}

git describe --tags --always --dirty
