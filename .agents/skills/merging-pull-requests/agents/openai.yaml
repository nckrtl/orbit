interface:
  display_name: "Merging Pull Requests"
  short_description: "Check approval, merge the PR, and clean up"
  default_prompt: "Use $merging-pull-requests to check CI, independent review, and maintainer approval, then merge this PR and clean up."
