interface:
  display_name: "Closing Out Pull Requests"
  short_description: "Verify an external merge, promote, and clean up"
  default_prompt: "Use $merging-pull-requests after the external orchestrator merges the exact second-approved removal head to verify the merge, promote its proof, and clean up."
