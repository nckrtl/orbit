interface:
  display_name: "Closing Out Pull Requests"
  short_description: "Verify the merge and clean the selected flow"
  default_prompt: "Use $merging-pull-requests after the external orchestrator merges the exact approved candidate to verify lineage and evidence under its delivery flow, then clean up; snapshot refresh applies only to proof flow."
