interface:
  display_name: "Resolve Pipeline Issues"
  short_description: "Resolve or propose fixes for unready Orbit issues"
  default_prompt: "Use $resolve-pipeline-issues to resolve Orbit pipeline issues, or return a read-only proposal for one exact Blocked or Backlog issue."
